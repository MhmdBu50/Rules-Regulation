@{
    ViewData["Title"] = "Bad Request";
    Layout = "_Layout";
}

<div class="container-fluid d-flex justify-content-center align-items-center min-vh-100">
    <div class="text-center">
        <div class="error-content">
            <div class="error-number mb-4">
                <h1 class="display-1 text-warning">400</h1>
            </div>
            <div class="error-text">
                <h2 class="mb-3">Bad Request</h2>
                <p class="lead mb-4">
                    The request you made is invalid or cannot be processed.
                </p>
                <div class="error-details mb-4">
                    <div class="alert alert-warning">
                        <i class="fas fa-exclamation-triangle me-2"></i>
                        Common causes:
                        <ul class="mt-2 mb-0 text-start">
                            <li>Invalid query format</li>
                            <li>Missing required information</li>
                            <li>Unauthorized operation attempted</li>
                        </ul>
                    </div>
                </div>
                <div class="error-actions">
                    <a href="@Url.Action("homePage", "Home")" class="btn btn-primary btn-lg me-3">
                        <i class="fas fa-home me-2"></i>Go to Homepage
                    </a>
                    <button onclick="history.back()" class="btn btn-outline-secondary btn-lg">
                        <i class="fas fa-arrow-left me-2"></i>Go Back
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
.error-content {
    max-width: 700px;
    margin: 0 auto;
}

.error-number h1 {
    font-size: 8rem;
    font-weight: bold;
    color: #ffc107;
    text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
}

.error-text h2 {
    color: #333;
    font-weight: 600;
}

.error-text p {
    color: #666;
    font-size: 1.1rem;
}

.error-details ul {
    list-style-type: none;
    padding-left: 0;
}

.error-details li {
    padding: 5px 0;
}

.error-details li:before {
    content: "• ";
    color: #ffc107;
    font-weight: bold;
}

.error-actions .btn {
    min-width: 150px;
}
</style>

<script>
// Mobile responsive adjustments
if (window.innerWidth <= 768) {
    document.addEventListener('DOMContentLoaded', function() {
        const errorNumber = document.querySelector('.error-number h1');
        if (errorNumber) {
            errorNumber.style.fontSize = '5rem';
        }
        
        const buttons = document.querySelectorAll('.error-actions .btn');
        buttons.forEach(btn => {
            btn.style.display = 'block';
            btn.style.width = '100%';
            btn.style.marginBottom = '10px';
            btn.classList.remove('me-3');
        });
    });
}
</script>
